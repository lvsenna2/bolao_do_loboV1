# Components

Reusable interface components live here.

- `ui`: base controls such as buttons, inputs, badges, tables and dialogs.
- `layout`: navigation, shells and structural components.
- `feedback`: loading, empty, error and toast-related components.

Feature-specific components should stay inside `src/features/<feature>/components`
until they are reusable across the application.
